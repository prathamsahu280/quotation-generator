# docgen — stateless document-generation microservice
fastapi
uvicorn[standard]
pydantic
httpx                  # Gemini REST calls
python-dotenv          # load docgen/.env locally
python-docx
pypdf

# PDF letterhead -> .docx background (scanned/designed letterheads)
pymupdf                # fitz: rasterise PDF page
pillow                 # save the page image
numpy                  # detect header/footer margins

# PDF rendering: LibreOffice (Linux, auto-detected) or docx2pdf (Windows/macOS + Word)
docx2pdf
